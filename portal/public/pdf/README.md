# PDF-образцы (не в git)

Файлы `1@1_*.pdf` в репозиторий не входят.

Для локального просмотра образцов скопируйте PDF из своей папки, например:

```bash
cp "/Users/vadim/Desktop/Формы ОКО/ОКО-pdf/"1@1_*.pdf .
```

Кнопка «Образец PDF» в портале работает только при наличии файлов в этой папке.
