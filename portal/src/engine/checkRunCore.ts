/** Re-export shared check-run core (`@oko/engine`). */
export {
  evalContextFromInstances,
  formsUsedByFormChecks,
  isFormNumericallyEmpty,
  latestInstancePerTemplate,
  runFormChecksWithData,
  type CheckMode,
  type CheckResultItem,
  type CheckRule,
  type CheckRunResult,
} from "@oko/engine";
