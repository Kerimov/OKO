.DS_Store
*.local
.oko-dev-pids
.oko-logs/

portal/node_modules
portal/dist
server/node_modules
server-nest/node_modules
server-nest/dist
packages/**/node_modules
packages/**/dist
desktop/tauri/node_modules
desktop/tauri/dist
desktop/tauri/src-tauri/target
desktop/tauri/release/

# Legacy Electron filler (not in repo; local leftovers must not flood SCM)
desktop/filler/

# Accidental tsc emit next to TypeScript sources
server/src/**/*.js

# SQLite (legacy local API file; kits use per-folder oko.db)
data/oko.db
data/oko.db-*

# PDF-образцы форм (локально, не публикуются в репозиторий)
portal/public/pdf/*.pdf

# Исходный комплект ОКО (бинарники)
reference/*.mdb
reference/*.mde
# Локальная копия комплекта ОКО (не публиковать)
12345/
*.mde
.env
*.pfx
*.pem
*.key

# Playwright
portal/test-results/
portal/playwright-report/
