.DS_Store
*.local
.oko-dev-pids
.oko-logs/

web/portal/node_modules
web/portal/dist
web/domain/node_modules
web/api/node_modules
web/api/dist
packages/**/node_modules
packages/**/dist
desktop/node_modules
desktop/dist
desktop/dist-install
desktop/src-tauri/target
desktop/release/

# Legacy Electron filler (not in repo; local leftovers must not flood SCM)
desktop/filler/
archive/desktop-filler/

# Accidental tsc emit next to TypeScript sources
web/domain/src/**/*.js

# SQLite (legacy local API file; kits use per-folder oko.db)
data/oko.db
data/oko.db-*

# PDF-образцы форм (локально, не публикуются в репозиторий)
web/portal/public/pdf/*.pdf

# Исходный комплект ОКО (бинарники)
archive/reference/*.mdb
archive/reference/*.mde
# Локальная копия комплекта ОКО (не публиковать)
archive/kits/12345/
*.mde
.env
*.pfx
*.pem
*.key

# Playwright
web/portal/test-results/
web/portal/playwright-report/
