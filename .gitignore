.DS_Store
*.local
portal/node_modules
portal/dist
portal/dist-offline
server/node_modules

# SQLite database
data/oko.db
data/oko.db-*

# PDF-образцы форм (локально, не публикуются в репозиторий)
portal/public/pdf/*.pdf
# Seed-комплект для offline-kit (генерируется при сборке)
portal/public/offline-package.json
portal/public/offline-kit-template.zip

# Исходный комплект ОКО (бинарники)
reference/*.mdb
reference/*.mde
# Локальная копия комплекта ОКО (не публиковать)
12345/*.mdb
*.mde
.env
